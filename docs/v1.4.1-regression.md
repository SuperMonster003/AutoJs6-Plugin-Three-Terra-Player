# v1.4.1 回归记录

记录日期: 2026/08/31

## 用户提供的 v1.4.0 实机结果

- 有线耳机: 单击 / 双击 / 三击全部正常.
- 无线耳机: 单击 / 双击 / 三击全部正常.
- TalkBack: 功能与语音播报符合预期.
- 阿拉伯语 RTL: 布局正常.
- 其余播放场景暂未发现问题.
- 唯一已知问题: 播放器右上角更多菜单在当前主题下显示为浅色面板与浅色文字, 无法辨认菜单项.

## v1.4.1 修复与模拟器回归

| 环境 | 结果 |
|---|---|
| Android API 33 x86_64 | 完整仪器测试 `21/21` 通过 |
| Android API 29 x86 | 更多菜单与持久化授权边界定向测试 `3/3` 通过 |
| JVM policy 测试 | `:app:testDebugUnitTest` 通过 |
| 文档门禁 | `MARKDOWN_OK languages=10 artifacts=36 mode=check` |
| R8 release | `:app:appendDigestToReleasedFiles` 通过, API 33 覆盖安装成功 |

更多菜单验证:

- 工具栏显式使用独立的浅色 / 深色 popup theme, 不再继承只适用于工具栏图标的 Dark ActionBar overlay.
- 浅色模式实拍确认 `Settings` 为深色文字配浅色面板.
- API 29 与 API 33 自动测试分别解析 popup 的 `textColorPrimary` 与 `colorSurface`, 两种夜间配置的对比度均不低于 `4.5:1`.

独立会话恢复端到端验证:

1. 通过 Android 系统文件选择器多选两首测试音频, 确认应用取得两项长期只读 URI 授权.
2. 切换到队列第 2 首, 定位到 `45.022 s`, 设置单曲循环与 `1.5x` 倍速.
3. 强制结束应用进程, 再从桌面启动器冷启动.
4. 界面恢复为第 2 首, `0:45 / 1:30`, 队列数 `2`, 单曲循环, `1.5x`; 主按钮显示 `Play`, 证明恢复后保持暂停且不会突然出声.
5. 无长期 URI 授权的 `content://` 队列由仪器测试确认会被拒绝并清除; Host Session 与临时外部路由不会写入恢复记录.

最终 R8 包 `autojs6-plugin-three-terra-player-v1.4.1-2fbfe8b6.apk` 为 `2,768,505` bytes, SHA-256 为 `A36B5B359ED72E118A2F1B752D7BB9A48B90A617190347F3E3E5938E6BA0AA42`. APK Signature Scheme v2, ZIP 对齐, 包名 `io.github.supermonster003.autojs6.plugin.audioplayer`, `versionName=1.4.1`, `versionCode=13` 均验证通过. 将该最终 R8 包覆盖安装到 API 33 后再次冷启动, 上述第 2 首 / 0:45 / 单曲循环 / 2 首队列 / 1.5x / 暂停状态仍完整恢复.

## 在线发布复核

- 公开仓库: [SuperMonster003/AutoJs6-Plugin-Three-Terra-Player](https://github.com/SuperMonster003/AutoJs6-Plugin-Three-Terra-Player).
- Release: [v1.2.1–v1.4.1](https://github.com/SuperMonster003/AutoJs6-Plugin-Three-Terra-Player/releases) 共五个历史 / 当前版本; GitHub API 回读确认每个条目均为正式发布且只包含一个 APK, 文件名, 大小与 SHA-256 全部匹配本地归档. v1.2.1 因历史中未保留独立源码提交, 标签锚定到紧随其后的 v1.2.2 兼容快照, 该情况已在 Release 说明中明确披露; APK 本身为重新验证过的 build 9 原始归档.
- 官方插件中心: [索引提交 `abbefcd`](https://github.com/SuperMonster003/AutoJs6-Official-Plugins-Index/commit/abbefcd196e29947e0d9acd56fec0317f5339952); 从线上 raw 索引回读确认稳定包名, `explorer-action`, `audio-player`, `default`, v1.4.1/build 13 与 APK SHA-256 均正确.

## 尚待完成

- 将 v1.4.1 R8 release APK 安装到真实设备, 对右上角更多菜单和上述冷启动恢复场景做一次复测. 在此完成前, Roadmap 的“v1.4.0 实机抽样验证”保持未勾选.
