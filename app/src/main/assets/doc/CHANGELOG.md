******

### 版本历史

******

# v1.5.0

###### 2026/08/31

* `新增` 专辑封面现在会为播放器渐变背景, 工具栏, 控件与队列实时生成可读色板; 各页面以边到边方式融入状态栏, 刘海与手势区域
* `新增` 播放设置新增可保存的默认倍速, 5 / 10 / 15 / 30 秒快退快进步长, 以及队列播完后的停止, 回到队首并暂停或自动重播行为
* `新增` 新增克制的播放 / 暂停与封面过渡动画, 并为关键播放操作加入遵循系统设置的触感反馈; 系统关闭动画时所有状态直接切换
* `修复` 语言与夜间模式对话框的列表项改用 16sp Material Body1, 不再沿用过大的平台列表文字
* `优化` 封面解码与最大 64x64 取色采样移至后台线程; 任意封面颜色继续通过正文 4.5:1 与轮廓 3:1 的既有对比度门禁
* `优化` 完成播放可视化可行性调研与 RMS 分桶原型: 平台 Visualizer 因要求录音权限而继续排除, 并记录无权限 Media3 PCM 抽头供后续性能基线验证

# v1.4.1

###### 2026/08/31

* `新增` 独立应用现在会在从启动器再次打开时恢复上次队列, 当前曲目, 停止位置, 循环模式, 随机状态与倍速; 恢复后保持暂停, 且仅保存仍有长期只读授权的系统文件选择器队列
* `修复` 修复播放器右上角更多菜单在部分主题下出现白底白字, 无法辨认设置菜单项的问题
* `优化` 主题色设置中的 AutoJs6 来源统一表述为 `跟随 AutoJs6`; 颜色选择面板直接显示宿主颜色的 HEX 值
* `优化` 完成十语言 README, CHANGELOG 与插件说明的基础人工复核, 并将项目主页与应用内更新地址迁移到 3-Terra Player 正式仓库

# v1.4.0

###### 2026/08/29

* `新增` 系统媒体通知全面升级: 新增上一曲 / 下一曲 / 随机播放开关与退出按钮, 使用应用专属的单色小图标, 随机状态与播放器实时同步
* `修复` 修复插件可能被 AutoJs6 插件中心自动标记为错误并禁用的问题: 插件信息与文件管理器动作现在使用各自独立的服务端点
* `修复` 清空播放队列后不再残留过期信息: 界面进入明确的空状态, 播放, 跳转, 倍速, 定时器与 A-B 等控件同步禁用
* `修复` 修复播放按钮阴影被底部区域裁切的问题; 设置与更新对话框中的单选, 多选, 进度条与按钮统一跟随主题色
* `优化` 应用与插件正式更名为 3-Terra Player: 应用 ID 保持不变, 可直接覆盖安装升级, 既有设置不受影响
* `优化` 播放器右上角菜单精简为仅保留设置入口, 移除与设置页重复的调色盘按钮

# v1.3.0

###### 2026/08/29

* `新增` 新增独立应用模式: 无需 AutoJs6 即可从启动页一次选择最多 128 个音频文件连续播放
* `新增` 新增设置页面: 语言, 夜间模式, 主题色, 断点续播, 检查更新, 发行历史与应用信息集中管理; 语言与外观默认跟随 AutoJs6, 宿主不可用时自动回退到内置默认值
* `新增` 新增手动与每日自动检查更新, 支持忽略指定版本, 并内置多语言发行历史
* `修复` 修复主题色跟随 AutoJs6 时始终提示宿主颜色不可用的问题
* `修复` 文件管理器中所有被宿主识别为音频的条目 (新增 WMA) 现在都可以直接调用本插件播放
* `优化` 断点续播只记住最近打开的一个文件: 打开其他文件或播放完毕后自动清除旧记录
* `优化` 播放界面固定保留三行信息区域并补足采样率与码率显示, 切歌与异步加载时布局不再抖动
* `优化` A-B 循环改为三次点击依次完成设 A 点, 设 B 点与清除; 底部控制区的间距与图标对齐同步优化

# v1.2.2

###### 2026/08/27

* `修复` 修复覆盖安装新版插件后, 文件管理器中所有音频都提示音频请求无效的问题: 入口现在兼容运行中宿主缓存的旧版只读请求
* `修复` 修复部分设备 MIME 表异常导致 `ogg`, `opus` 等已支持扩展名被误拒的问题: 由扩展名白名单提供稳定的规范音频类型
* `优化` 请求被拒绝时输出隐私安全的原因码日志 (不含文件名, 路径与 URI), 便于反馈与定位问题

# v1.2.1

###### 2026/08/27

* `新增` 从文件管理器打开一首歌时, 自动发现同文件夹的其他音频并按文件名自然排序连播: 最多 128 首, 从所选曲目开始
* `修复` 修复 Android 7.0 设备上系统自动附加的窗口标志导致合法播放请求被误拒的问题
* `修复` 修复自动切换到同文件夹曲目时可能崩溃的问题
* `优化` 同目录发现全程只读且有界: 不递归子目录, 不猜测文件地址, 不申请存储权限, 与音频同名的视频文件不会进入队列
* `优化` 同目录读取会话交由后台播放服务持有, 并在队列替换, 播放完成或服务销毁时自动关闭
* `依赖` 内置文件管理器动作接口升级至向后兼容的 v12, 最低宿主版本保持 build 5276

# v1.2.0

###### 2026/08/27

* `新增` 新增多选播放: 在文件管理器中勾选最多 128 个音频一键加入队列, 严格保持勾选顺序
* `新增` 新增上一曲 / 下一曲控制, 可跳转与移除曲目的队列面板, 以及顺序 / 随机 / 单曲循环模式
* `新增` 新增睡眠定时器: 15 / 30 / 60 分钟预设或自定义时长, 支持播完本曲停止与最后 5 秒淡出
* `新增` 新增 A-B 区间循环, 可反复聆听选定片段
* `优化` 系统媒体控制新增上一曲, 下一曲与快退快进 10 秒命令, 通知元数据随队列切换实时更新
* `优化` 文件管理器请求校验扩展到有序多选目标, 继续保持最小只读授权
* `依赖` 文件管理器动作接口从 v2 升级至 v4, 最低宿主版本提升至 build 5276
* `依赖` 新增 AndroidX RecyclerView 1.4.0

# v1.1.0

###### 2026/08/27

* `新增` 全新播放界面: 专辑封面, 标题 / 艺术家 / 专辑标签, 技术信息与可拖动进度条
* `新增` 新增快退快进 10 秒, 单曲循环开关与 0.5 至 2 倍变速播放
* `新增` 自动读取音频内嵌标签与封面并同步到系统媒体通知
* `新增` 新增断点续播: 重新打开同一文件时从上次位置继续, 播放完成后自动清除
* `优化` 播放结束或解码失败后可在界面内直接重新播放, 无需返回文件管理器
* `优化` 播放错误提示附带具体错误代码, 便于排查与反馈
* `依赖` 新增 AndroidX ConstraintLayout 2.2.1
* `依赖` 移除不再使用的 AndroidX Media3 UI 依赖

# v1.0.1

###### 2026/08/08

* `修复` 修复在插件中心启用插件时出现空服务绑定的问题
* `优化` 插件名称, 描述与使用说明更加简洁自然

# v1.0.0

###### 2026/08/02

* `新增` 首个正式版本: 为 AutoJs6 文件管理器提供播放音频动作, 覆盖 18 种常见音频扩展名, 点击即可播放
* `新增` 基于 Media3 ExoPlayer 与 MediaSessionService 的稳定播放: 支持后台播放, 音频焦点管理, 输出设备断开自动暂停与系统媒体控制
* `新增` 支持接收来自其他应用的只读音频打开请求; 解码失败时可转交其他兼容应用并防止自循环
* `新增` 严格的安全基线: 不申请存储与互联网权限, 仅接受并转发最小只读授权
* `新增` 插件信息, 使用说明, README 与更新日志覆盖 10 种语言
* `依赖` 新增 AndroidX Media3 ExoPlayer, Session 与 UI 1.10.1
