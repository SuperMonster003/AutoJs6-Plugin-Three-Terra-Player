# Roadmap

音频播放器插件 (AutoJs6-Plugin-Audio-Player) 功能与体验升级路线图.

针对用户集中反馈的 "功能过于单一" 与 "毫无体验感可言" 两类问题, 以版本里程碑为单位规划可逐项验收的改进项. 每个条目均包含验收标准, 完成后勾选对应复选框并在发布说明中体现.

******

### 现状与问题定位

******

v1.0.x 的能力与短板:

- 播放界面仅有工具栏加 Media3 默认控制条, 无封面, 无艺术家 / 专辑信息, 视觉上接近空白页 (对应反馈 "毫无体验感可言").
- 除播放 / 暂停 / 拖动外没有任何控制能力: 无倍速, 无循环, 无快退快进, 无进度记忆, 无定时停止 (对应反馈 "功能过于单一").
- 播放结束或解码失败后服务立即结束, 界面失去响应, 想重听必须回到文件管理器重新打开.
- 音频内嵌标签 (ID3 等) 与封面完全未利用, 系统媒体通知只显示文件名.
- 一次动作只能播放一个文件, 无法连播同目录的其他音频.

安全与验证层 (policy 体系, 只读转发, 最小授权) 是现有优势, 所有新功能沿用该体系, 不放宽任何边界.

******

### 里程碑总览

******

| 里程碑 | 版本 | 主题 | 状态 |
|---|---|---|---|
| M1 | v1.1.0 | 播放体验重构: 全新界面与核心播放控制 | 已交付 (2026/08/27, 待实机抽样验证) |
| M2 | v1.2.0 | 连播与队列: 显式多选队列, 定时停止, 通知增强 | 已交付 (2026/08/27; 当时的同目录阻塞已由 M2.1 解除) |
| M2.1 | v1.2.1 | Host Session 单曲同目录发现与自动连播 | 工程与本地部署完成 (2026/08/27; 在线发布待授权) |
| M2.2 | v1.2.2 | 文件管理器入口热升级与设备 MIME 兼容修复 | 工程与本地部署完成 (2026/08/27; 在线发布待授权) |
| M3 | v1.3.0 | 个性化与视觉: 单色源主题, 动态取色, 设置页, 沉浸式界面 | 进行中 (独立应用、设置、主题跟随与播放界面精修已完成; 动态取色等后续项保留) |
| M4 | v2.0.0 | 进阶能力: 歌词, 均衡器, 协议演进跟进 | 未开始 |

约束结论: M2 调研确认单靠宿主 `AppFileProvider` 的父目录 URI 不能枚举子项; Explorer Action v12 随后以显式可选的 `READ_SIBLINGS` Host Session 补齐这一能力. v1.2.1 仅通过 UID 固定会话分页读取目标的直接同级文件, 不猜测 URI, 不接触明文路径, 不申请存储权限; 无该能力时仍安全回退为单文件. 最低宿主永久保持 `AutoJs6 6.8.0 build 5276` 与 Explorer Action v12, 详见 [队列可行性调研](docs/explorer-action-queue-feasibility.md).

******

### M1 — v1.1.0 播放体验重构

******

目标: 让 "打开一个音频" 从一个白板页面变成完整的现代播放器体验, 一次性补齐最基础的播放控制能力.

#### 界面重构

- [x] **全新播放界面布局**: 封面卡片, 标题, 艺术家 / 专辑副标题, 技术信息行, 进度条与时间标签, 传输控制行的完整版式. — 验收: 竖屏与横屏均正常显示, 深色模式正常, 无封面时显示占位图.
- [x] **专辑封面展示**: 读取音频内嵌封面并显示, 无封面时显示音符占位图. — 验收: 含内嵌封面的 mp3 / flac 显示封面, 无封面文件显示占位图, 超大封面不引起卡顿或崩溃 (封面统一重编码为不超过 512px 的 JPEG).
- [x] **媒体标签展示**: 标题 / 艺术家 / 专辑取自内嵌标签, 无标签时回退为文件名. — 验收: 有标签文件三项均显示, 无标签文件仅显示文件名且副标题隐藏.
- [x] **音频技术信息行**: 显示采样率, 编码格式, 比特率 (缺项自动省略), 并固定保留标题 / 标签 / 技术信息三行高度. — 验收: mp3 显示形如 `44.1 kHz · MP3 · 320 kbps`; 元数据异步补齐时布局不抖动, 信息完全缺失时保留透明占位.
- [x] **进度条与时间显示**: 可拖动进度条, 当前位置与总时长标签, 拖动中实时预览位置. — 验收: 拖动松手后从目标位置继续播放, 时长未知时进度条禁用且显示 `--:--`.

#### 播放控制

- [x] **播放 / 暂停主按钮**: 大尺寸按钮, 状态图标随播放状态切换, 缓冲时显示加载指示. — 验收: 点按切换播放与暂停, 图标与内容描述同步更新.
- [x] **快退 / 快进 10 秒**: 界面按钮与系统媒体控制共用同一增量. — 验收: 点按后位置变化 10 秒, 边界处不越界.
- [x] **单曲循环开关**: 循环关闭与单曲循环两种模式切换, 图标区分状态. — 验收: 开启后播放结束自动从头继续, 关闭后播放结束正常停止.
- [x] **倍速播放**: 0.5× / 0.75× / 1× / 1.25× / 1.5× / 2× 菜单选择, 按钮实时显示当前倍速. — 验收: 各档位生效且音调保持正常, 按钮文本与实际速度一致.
- [x] **播放结束后原地重播**: 播放结束或服务停止后, 播放按钮可直接重新开始播放, 无需回到文件管理器. — 验收: 自然播完后点击播放按钮从头播放成功.

#### 播放增强

- [x] **播放进度记忆**: 仅保存最近打开的一个文件; 再次打开同一文件时续播, 打开任何其他文件时立即清除旧记录, 播放完成不保存; 最长保留 30 天. — 验收: `A → 退出 → A` 恢复 A, `A → 退出 → B` 在 B 打开时清除 A, 任意曲目播完后再打开都从头播放.
- [x] **媒体通知增强**: 系统媒体通知显示标签标题与封面 (由充实后的媒体元数据自动同步). — 验收: 含标签文件的通知显示标签标题与封面图.
- [x] **错误反馈增强**: 解码失败时错误面板附带具体错误代码, 保留 "使用其他应用打开" 降级入口, 出错后可直接重试 (服务出错后保留 30 秒供界面呈现与重试). — 验收: 播放损坏文件时显示错误代码, 点击播放按钮可重试, 外部打开入口可用.

#### 工程配套

- [x] **新增纯逻辑 policy 单元测试**: 进度记忆策略与音频信息格式化策略全部分支覆盖 (`PositionMemoryPolicyTest`, `AudioInfoPolicyTest`). — 验收: `:app:testDebugUnitTest` 全部通过.
- [x] **多语言资源同步**: 新增界面字符串覆盖全部 11 个语言目录. — 验收: 各语言目录字符串键集合一致, 构建无缺失资源告警.
- [x] **版本与文档发布**: 版本号 1.1.0, changelog 与 README 十语言重新生成, 产出签名 release APK 至 `releases/`. — 验收: `releases/` 出现 v1.1.0 APK (`autojs6-plugin-audio-player-v1.1.0-623309b9.apk`), README 版本历史包含 v1.1.0.
- [ ] **实机抽样验证**: 在真实设备上安装 v1.1.0, 按上述各项验收标准逐条走查 (含 R8 release 包功能完整性). — 验收: 走查记录留档, 发现问题回归修复.

******

### M2 — v1.2.0 连播与队列

******

目标: 突破 "一次只能听一个文件" 的最大功能瓶颈, 补齐时间类控制. 当前宿主无法从单文件动作枚举同目录子项, 因此以协议 v4 的显式多选队列作为安全可落地方案.

- [x] **[调研] 父目录子项枚举可行性**: 已核对 AutoJs6 build 5269 / 5276 / 当前开发分支及 AndroidX FileProvider 实现, 输出 [独立调研报告](docs/explorer-action-queue-feasibility.md), 含适配矩阵, 安全降级和重新评估条件. — 验收: 报告明确结论为“当前不可枚举”, 协议 v4 显式多选为可落地路径.
- [x] **单文件触发同目录自动连播**: v1.2.1 通过 Explorer Action v12 的可选 `READ_SIBLINGS` Host Session 实现; 无会话或发现失败时保持单文件行为, 不以 URI 猜测绕过. — 验收: 直接同级音频按稳定自然文件名顺序组成队列, 所选曲定位正确, 非音频及同名 `.mp4` 不进入队列.
- [x] **协议 v4 显式多选队列入口**: 新增 `play-audio-selection` 多选工具栏动作, 接收宿主有序 `TARGETS`, 一次验证并播放 1–128 个不重复音频文件; `play-audio` 单文件入口保持可用. — 验收: 动作目录同时暴露 single / multiple 两个动作, 顺序完整保留, 非法目标或授权整体拒绝.
- [x] **上一曲 / 下一曲控制**: 界面与系统媒体控制均使用 Media3 原生播放列表切换相邻音频. — 验收: 两处切换行为一致, 标题 / 封面 / 技术信息 / 续播位置同步刷新.
- [x] **播放队列面板**: 展示显式选中的待播列表, 支持点选跳转与移除, 当前曲目有标记. — 验收: 列表与实际播放顺序一致, 点选立即切换, 移除后索引与元数据保持正确.
- [x] **随机与顺序模式**: 顺序 / 随机 / 单曲循环三态整合, 模式直接映射 Media3 shuffle 与 repeat 状态. — 验收: 三种模式行为正确且状态图标与内容描述同步.
- [x] **睡眠定时器**: 15 / 30 / 60 分钟与 1–1440 分钟自定义时长, 最后 5 秒淡出停止; 支持“播完本曲后停止”及取消. 定时状态由服务持有, 界面退出后继续有效. — 验收: 策略边界单测通过, 到时清理循环状态并停止, 取消后恢复音量且不再触发.
- [x] **A-B 区间循环**: 设置 A / B 两个时间点循环播放区间, 切曲时自动清除, 最小区间 500 ms. — 验收: 策略边界单测通过, 区间内轮询回跳稳定, 清除后恢复正常播放.
- [x] **通知栏快退 / 快进按钮**: 通过媒体按钮偏好向系统媒体控制声明上一曲, 下一曲与 ±10 秒按钮. — 验收: MediaSession 构建和 Android 测试编译通过, Android 13+ 显示与操作纳入实机抽样项.
- [ ] **耳机 / 蓝牙按键适配验证**: 单击 / 双击 / 三击行为与线控协议一致. — 验收: 实机验证记录留档.
- [x] **M2 策略与契约测试**: 新增 `PlaybackModePolicyTest`, `SleepTimerPolicyTest`, `AbLoopPolicyTest`, `ExplorerQueuePolicyTest`, v4 Intent 严格验证测试及私有队列契约往返 / 篡改测试. — 验收: 42 个 JVM 单测全通过; 8 个 Android 仪器化测试分别在 API 24 与 API 25 模拟器上 8 / 8 通过; Lint 为 0 error.
- [x] **M2 多语言资源与说明**: 新增队列, 模式, 定时器, A-B 与多选动作字符串, 覆盖全部 11 个 Android 语言目录; README / changelog JSON 源和插件中心说明同步更新. — 验收: 各语言默认可回退, JSON 全部可解析, Android 资源编译通过.
- [ ] **M2 实机抽样验证**: 在 AutoJs6 build 5276+ 逐项验证单选 / 多选入口, 队列切换与移除, 后台定时, A-B, Android 13+ 通知按钮及有线 / 蓝牙媒体键. — 验收: 走查记录留档, 发现问题回归修复.
- [x] **M2 版本与发布**: 版本号 1.2.0 (build 8), 十语言文档生成, Debug / R8 Release 验证, 产出并在 API 24 模拟器成功安装带摘要签名 APK. — 验收: `releases/autojs6-plugin-audio-player-v1.2.0-3e2c082f.apk` (2,509,308 bytes), APK Signature Scheme v2 验证通过, SHA-256 `B30ABC968BBF4CD0AC40E0D646E516F54433FEDB43EF1BF2FEB6F07A1FE043D9`.

******

### M2.1 — v1.2.1 Host Session 单曲同目录发现

******

目标: 在不扩大存储权限的前提下, 让文件管理器中打开一首歌即可获得同目录自然排序队列, 同时保持 v1.2.0 显式多选入口完全兼容.

- [x] **Explorer Action v12 能力声明**: 单文件 `play-audio` 声明 `READ_SIBLINGS`, 显式多选动作不声明; 插件继续要求 `AutoJs6 >= 6.8.0 build 5276`, 后续新增能力也不提高该门槛. — 验收: catalog 与严格 Intent 契约测试覆盖协议、宿主版本、能力开关和 Binder 篡改拒绝.
- [x] **直接同级安全发现**: 仅分页枚举可读、非符号链接的直接文件, 音频扩展名白名单过滤, 稳定自然排序, 上限 128 首, 永不递归. — 验收: 所选文件始终保留且索引正确; `.mp4` 等同名伴生视频不进入队列.
- [x] **受控媒体读取**: 所选曲继续使用宿主授予的原始 `content://` URI, 其余歌曲使用插件私有 `autojs6-explorer://audio/<index>` 路由并按需从 Host Session 获取只读 PFD; Activity / Service 明确接管并关闭会话. — 验收: 私有契约拒绝路由、相对文件名、Binder 或 ClipData 篡改.
- [x] **队列交接稳定性**: MediaSession 的回到播放器 Intent 保留“原始授权目标”与“当前播放项”两个独立状态, 自动切到同级歌曲时不重写授权锚点. — 验收: 第 1 首自然播完后自动切到第 2 首, Session Activity 更新不崩溃.
- [x] **API 24 网关兼容**: 仅将系统从 `excludeFromRecents` manifest 属性复制到已交付 Intent 的 `FLAG_ACTIVITY_EXCLUDE_FROM_RECENTS` 纳入已知非授权标志白名单; READ / PREFIX 仍必需, WRITE / PERSISTABLE 与未知位继续拒绝. — 验收: API 24 实际 Intent `0x800081` 被接受, 安全标志单元测试覆盖允许与拒绝分支.
- [x] **联动设备验收**: 在 AutoJs6 6.8.0 build 5276 / API 24 模拟器中使用两组真实命名的 `.m4a` + 同名 `.mp4` 目录验证. — 验收: 队列仅显示 2 首 `.m4a`, 自然切曲后播放器保持存活; 46 项 JVM 与 9 项 Android 仪器化契约测试零失败.
- [x] **版本与十语言文档**: `version.properties` 升级至 1.2.1 (build 9), changelog / README / 插件说明同步并重新生成. — 验收: JSON 可解析, Android 11 个语言资源目录可编译; R8 签名 APK 已归档至 `releases/autojs6-plugin-audio-player-v1.2.1-f3b879d3.apk` (2,530,852 bytes), CRC32 `F3B879D3`, SHA-256 `B3035CD00383C615E32364BA7F1329968A813C5CD2D86C2ACF2F3751BCA58CD6`, APK Signature Scheme v2 校验通过并已在 API 24 模拟器覆盖安装核对版本.
- [ ] **在线发布**: 创建 GitHub Release 并同步宿主插件中心元数据. — 验收: 需另行取得发布授权后执行; 当前仅完成本地归档, 未推送或发布.

******

### M2.2 — v1.2.2 文件管理器入口兼容修复

******

目标: 修复覆盖安装插件后 AutoJs6 文件管理器中的播放按钮会对所有音频提示“音频请求无效”的阻断问题, 并关闭不同 Android MIME 表导致的同类入口缺口.

- [x] **原始设备根因复现**: 在问题设备 `QV770340J7` (API 33) 保留原始 18:10:41 日志并复测 MP3 / OGG; 确认请求在 Media3 之前被网关拒绝. — 验收: 隐私安全拒绝码准确命中 `PROTOCOL_VERSION`; 重启 AutoJs6 后同一 OGG 立即进入播放器, 证明运行中动作目录缓存是触发条件.
- [x] **协议热升级兼容窗口**: 插件继续对外声明 Explorer Action v12, 入口接受结构兼容的只读 v4–v12 请求, 解决宿主在插件覆盖安装后仍短暂发送 v1.2.0 缓存 v4 描述的问题. — 验收: v4 与 v12 单文件请求通过, v3 与 v13 拒绝; URI, ClipData, READ / PREFIX、禁止写入 / 持久授权及 Host Session 校验保持不变.
- [x] **设备 MIME 表兼容**: 广告扩展名与规范音频 MIME 映射改为单一策略源; 对平台返回的通配符或 `application/ogg` 等非音频元数据按已声明扩展名安全规范化. — 验收: API 24 / 25 上原先失败的 `ac3`, `m4b`, `m4r`, `mp1`, `oga`, `ogg`, `opus`, `wave` 与其余已声明扩展全部通过入口契约测试.
- [x] **可诊断拒绝日志**: 严格入口返回稳定拒绝原因码, 日志不记录文件名、显示路径或 URI. — 验收: 原始失败场景输出 `AudioExplorerGateway: ... PROTOCOL_VERSION`, 合法请求不输出拒绝日志.
- [x] **跨版本与真实升级回归**: 在 API 24 / 25 / 37 三个模拟器运行完整 Android 契约测试, 并实际执行“安装 v1.2.0 → 启动宿主缓存 v4 → 不重启宿主覆盖安装修复版 → 播放 OGG”. — 验收: 50 / 50 JVM 单测通过, 三个系统各 12 / 12 仪器化测试通过, Lint 0 error / 37 warning; 缓存 v4 + `application/ogg` 请求被规范化为 `audio/ogg` 并进入播放器.
- [x] **版本与十语言文档**: 版本升级至 1.2.2 (build 10), changelog / README 重新生成. — 验收: JSON 可解析, Debug / R8 Release 构建及签名验证通过; `releases/autojs6-plugin-audio-player-v1.2.2-48d2f114.apk` (2,537,916 bytes), CRC32 `48D2F114`, SHA-256 `76442C74D0720D7AF0FF3FF89CC07C3146F10D5D5F81198D406AAC31D5ADB3FE`, APK Signature Scheme v2 与 ZIP 对齐校验通过, 并在原问题 API 33 设备覆盖安装后实测 OGG 播放状态为 `state=3` 且进度持续增长.
- [ ] **在线发布**: 创建 GitHub Release 并同步宿主插件中心元数据. — 验收: 需另行取得发布授权后执行; 当前仅完成本地归档, 未推送或发布.

******

### M3 — v1.3.0 个性化与视觉

******

目标: 从 "可用" 到 "好看好用", 建立插件自己的视觉气质.

- [x] **单色源主题配色系统**: 默认经官方只读契约跟随 AutoJs6 主色, 另提供完整 Material 500 预置组与 `#RRGGBB` 自定义色; 使用 HCT tonal palette 从唯一 seed 生成亮色 / 暗色语义角色, 对应用栏, 表面, 容器, 轮廓, 控件与错误状态统一着色. — 验收: 19 个预置及黑 / 白极端 seed 的双模式测试均满足主文字 7:1, 普通文字 4.5:1, 控件轮廓 3:1; 宿主色变化或本地选择变化时不中断播放并自动刷新. 机制详见 [单色源主题配色系统](docs/theme-color-system.md).
- [x] **播放控制视觉精修**: 五个传输控制采用等宽槽位并保证播放按钮绝对居中; ±10 秒图标改为与上一曲 / 下一曲同尺寸的双三角; 最下排工具按钮增加容器色, 高对比前景与轮廓, 并区分普通 / 激活 / 禁用状态. — 验收: 亮色与暗色主题下底部按钮内容对比度至少 4.5:1, 边界对背景至少 3:1, 320dp 及以上宽度保持五等分.
- [x] **独立应用与设置**: 新增启动器入口及 1–128 个文档选择, 提供语言、夜间模式、主题色、续播、手动 / 每日更新检查、已忽略版本管理、发行历史及关于页. — 验收: 没有 AutoJs6 也可完整启动和播放; 设置持久化且完整覆盖 11 个 Android 语言资源目录.
- [x] **跟随 AutoJs6 外观契约**: 通过受签名权限保护的官方只读提供器获取语言、夜间模式和主题色; 插件信息服务补充 `org.autojs.plugin.INFO` 入口. — 验收: 官方签名且启用的宿主返回 `AVAILABLE`; 未安装或禁用时“跟随 AutoJs6”仍显示为禁用态, 并回退至系统外观与内置默认色.
- [x] **播放稳定性与可发现性**: 元数据区域固定三行占位, 技术信息结合标签与实际选中音轨补足采样率 / 比特率; A-B 按钮改为“设 A → 设 B → 清除”三次点击循环, 底部与传输控制增加间距并校正图标. — 验收: 异步标签加载和切曲时不再因 2 / 3 行切换抖动; A-B 每个阶段均有明确反馈.
- [x] **宿主音频路由与 WMA**: Explorer 动作目录除 19 个已知扩展名外同时声明 `audio/*`, 公开 Android 入口补充三个旧式 WMA / ASF MIME 别名. — 验收: 在宿主插件中心启用本插件后, 宿主识别为音频的任意条目都直接调用插件; 用户禁用插件时仍保留宿主 ACTION_VIEW 降级. 入口覆盖不等同于设备 / 播放引擎的解码能力.
- [ ] **封面动态取色**: 从封面提取主色调应用于界面背景渐变与控件强调色, 深浅色模式各自适配. — 验收: 不同封面呈现不同色调, 无封面时回退默认主题色, 对比度满足可读性.
- [ ] **沉浸式边到边界面**: 状态栏 / 导航栏透明融合, 适配刘海屏与手势区域. — 验收: Android 15 edge-to-edge 强制模式下显示正常.
- [ ] **高级播放设置**: 在已交付续播开关的基础上, 继续增加默认倍速, 快退快进步长 (5 / 10 / 15 / 30 秒), 完成后行为等偏好项. — 验收: 设置持久化且即时生效.
- [ ] **过渡动画与触感反馈**: 播放状态切换动画, 封面出场过渡, 关键操作触感反馈. — 验收: 动画流畅无掉帧, 系统关闭动画时优雅降级.
- [ ] **[调研] 播放可视化方案**: 评估波形 / 频谱展示路径 (系统 Visualizer 需要录音权限, 与零权限原则冲突; 评估基于解码数据的自绘方案). — 验收: 输出结论, 冲突不可解则记录放弃理由.
- [ ] **无障碍与 RTL 完整验证**: TalkBack 全流程可操作, 阿拉伯语 RTL 布局正确. — 验收: 验证清单留档, 问题清零.

******

### M4 — v2.0.0 进阶能力

******

目标: 差异化能力, 建立与通用播放器的区分度.

- [ ] **歌词支持**: 解析内嵌歌词标签; 调研通过前缀授权读取同目录同名 `.lrc` 文件; 同步滚动高亮显示. — 验收: 含内嵌歌词文件正确滚动显示, `.lrc` 路径依调研结论落地或降级.
- [ ] **均衡器与音效**: 基于音频会话的系统均衡器预设 (流行 / 摇滚 / 人声等) 与低音增强, 不新增权限. — 验收: 各预设听感差异明确, 不支持的设备优雅隐藏入口.
- [ ] **音频信息详情页**: 完整技术参数 (声道, 位深, 容器格式, 文件大小, 修改时间) 与标签全览, 支持复制. — 验收: 与实际文件参数一致.
- [x] **协议 v12 受限同级枚举跟进**: v1.2.1 已通过可选 Host Session 落地单曲同目录队列, 并保持 v4 显式多选兼容路径. — 验收: 无声明能力时宿主拒绝同级读取, 会话关闭后失效, 无会话时安全回退.
- [ ] **协议后续演进跟进**: 继续评估不提高 `build 5276` 门槛即可选用的更多来源界面与媒体能力. — 验收: 宿主提供相关稳定可选能力后一个迭代内完成评估与适配.
- [ ] **性能基线**: 冷启动至出声时长, 大文件 (500 MB+) 拖动响应, 内存占用基准与回归防线. — 验收: 基线数据留档, 回归项纳入发布前检查.

******

### 工程与质量 (持续项)

******

- [ ] **policy 层测试覆盖率保持 100% 分支覆盖** (新增策略随功能落地即配套测试).
- [ ] **仪器化测试扩展**: 播放服务生命周期, 进度记忆端到端, 通知行为.
- [ ] **release 构建混淆验证**: 每个里程碑发布前验证 R8 产物功能完整.
- [ ] **安全边界回归清单**: 每次触碰 intent / URI 转发路径时重跑既有 policy 测试并人工复查授权范围.

******

### 发布流程模板 (每个里程碑执行)

******

- [ ] `version.properties` 更新 `VERSION_NAME` 与 `VERSION_BUILD`
- [ ] `.changelog/lang_*.json` 十语言补充版本条目
- [ ] `.readme/lang_*.json` 功能清单同步 (如有新增)
- [ ] 运行 `.python/generate_markdown.py` 重新生成 README 与 CHANGELOG
- [ ] `:app:testDebugUnitTest` 全部通过
- [ ] `:app:assembleDebug` 构建通过并抽样安装验证
- [ ] `:app:appendDigestToReleasedFiles` 产出带摘要签名 APK 至 `releases/`
- [ ] Roadmap 勾选已完成项并更新里程碑状态
