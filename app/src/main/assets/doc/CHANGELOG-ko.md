******

### 릴리스 기록

******

# v1.6.0

###### 2026/09/13

* `기능` 로컬 재생 목록: M3U/M3U8, PLS, XSPF, WPL, ASX/WAX/WVX, MPCPL, DPL의 순서, 제목, 중복 항목을 유지. 호스트에서는 같은 폴더를 읽고, 독립 실행에서는 폴더를 선택해 상대 경로를 처리. 한 번에 한 목록, 최대 128개 항목. 네트워크 URL, HLS, 중첩 목록은 미지원
* `수정` 언어 및 야간 모드 대화상자 항목이 지나치게 큰 플랫폼 목록 글자 대신 16sp Material Body1을 사용합니다
* `개선` 다국어 리소스 통일, 명시적인 플러그인 활성화 및 릴리스 산출물 검증

# v1.5.0

###### 2026/09/12

* `기능` 앨범 아트에서 그라데이션, 도구 모음, 제어 버튼, 대기열에 적용할 읽기 쉬운 색상표를 실시간 생성합니다. 모든 화면은 상태 표시줄, 디스플레이 컷아웃, 제스처 영역을 고려한 엣지 투 엣지 방식으로 표시됩니다
* `기능` 재생 설정에 저장 가능한 기본 속도, 5 / 10 / 15 / 30초 되감기와 빨리 감기, 대기열 종료 시 정지 / 처음으로 돌아가 일시 정지 / 다시 재생 동작을 추가했습니다
* `기능` 재생 / 일시 정지와 앨범 아트에 절제된 전환을 추가하고 주요 재생 작업에 시스템 설정을 따르는 햅틱을 추가했습니다. 시스템 애니메이션을 끄면 상태를 즉시 전환합니다
* `수정` 언어 및 야간 모드 대화상자 항목이 지나치게 큰 플랫폼 목록 글자 대신 16sp Material Body1을 사용합니다
* `개선` 앨범 아트 디코딩과 최대 64x64 색상 샘플링을 메인 스레드 밖에서 처리합니다. 임의의 색상에도 본문 4.5:1과 윤곽선 3:1의 기존 대비 기준을 유지합니다
* `개선` 재생 시각화 연구와 RMS 버킷 프로토타입을 완료했습니다. 녹음 권한이 필요한 Visualizer는 계속 제외하고 권한 없는 Media3 PCM 탭을 향후 성능 기준 검증 경로로 문서화했습니다
* `개선` README 레이아웃과 Gradle 플랫폼 버전 관리 방식을 통일
* `개선` 플러그인 설명을 간결하게 다듬고 다국어 리소스의 문장 부호를 통일
* `개선` 외부 보기 진입점을 External Viewer로 변경해 뷰어 의미를 통일
* `개선` 업데이트 대화상자의 릴리스 기록 버튼에서 내장 릴리스 기록 페이지를 열도록 변경
* `개선` 빌드 시 의도하지 않은 네이티브 의존성을 거부하고 JSON 보고서 생성

# v1.4.1

###### 2026/08/31

* `기능` 독립 실행형 앱을 런처에서 다시 열면 마지막 대기열, 현재 곡, 정지 위치, 반복 모드, 셔플 상태, 재생 속도를 복원합니다; 복원된 세션은 일시 정지 상태를 유지하며 영구 읽기 권한이 있는 시스템 파일 선택기 대기열만 저장합니다
* `수정` 일부 테마에서 플레이어 오른쪽 위 더보기 메뉴가 흰색 배경에 흰색 글자로 표시되어 설정 항목을 알아볼 수 없던 문제를 수정했습니다
* `개선` 테마 색상 설정의 AutoJs6 소스 문구를 `AutoJs6 따르기`로 통일하고, 색상 선택 패널에는 호스트 색상의 HEX 값을 바로 표시합니다
* `개선` 10개 언어의 README, CHANGELOG, 플러그인 안내를 기본적으로 수동 검토하고 프로젝트 및 앱 내 업데이트 링크를 3-Terra Player 공식 저장소로 이전했습니다

# v1.4.0

###### 2026/08/29

* `기능` 시스템 미디어 알림 전면 개선: 이전 곡 / 다음 곡 / 셔플 전환 / 종료 버튼을 새로 갖추고 앱 전용 단색 아이콘을 사용하며 셔플 상태는 플레이어와 실시간으로 동기화
* `수정` AutoJs6 플러그인 센터에서 플러그인이 오류로 자동 표시되고 비활성화될 수 있던 문제 수정: 플러그인 정보와 파일 관리자 동작이 이제 각각 독립된 서비스 엔드포인트를 사용
* `수정` 재생 대기열을 비운 뒤 오래된 정보가 남던 문제 수정: 화면이 명확한 빈 상태로 전환되고 재생, 탐색, 배속, 타이머, A-B 등 컨트롤이 함께 비활성화
* `수정` 재생 버튼 그림자가 하단 영역에서 잘리던 문제 수정; 설정과 업데이트 대화 상자의 라디오 버튼, 체크박스, 진행 막대, 버튼이 일관되게 테마 색상을 따르도록 개선
* `개선` 앱과 플러그인 이름을 3-Terra Player로 공식 변경: 애플리케이션 ID는 그대로이므로 덮어쓰기 설치로 바로 업그레이드할 수 있고 기존 설정도 그대로 유지
* `개선` 플레이어 오른쪽 위 메뉴를 설정 항목만 남기고 간소화하고 설정 페이지와 중복되던 팔레트 버튼을 제거

# v1.3.0

###### 2026/08/29

* `기능` 독립 실행 모드 추가: AutoJs6 없이도 시작 화면에서 최대 128개의 오디오 파일을 한 번에 선택해 연속 재생
* `기능` 설정 페이지 추가: 언어, 야간 모드, 테마 색상, 이어서 재생, 업데이트 확인, 릴리스 기록, 앱 정보를 한곳에서 관리; 언어와 모양은 기본적으로 AutoJs6를 따르고 호스트를 사용할 수 없으면 내장 기본값으로 자동 대체
* `기능` 수동 및 하루 한 번 자동 업데이트 확인 추가: 특정 버전 무시를 지원하고 여러 언어로 된 릴리스 기록을 내장
* `수정` 테마 색상이 AutoJs6를 따를 때 호스트 색상을 사용할 수 없다고 계속 표시되던 문제 수정
* `수정` 파일 관리자에서 호스트가 오디오로 인식하는 모든 항목을 이제 이 플러그인으로 바로 재생 가능 (WMA 새로 추가)
* `개선` 이어서 재생은 가장 최근에 연 파일 하나만 기억: 다른 파일을 열거나 재생을 마치면 이전 기록을 자동으로 삭제
* `개선` 재생 화면이 세 줄 정보 영역을 항상 유지하고 샘플링 레이트와 비트레이트 표시를 보완해 곡 전환이나 비동기 로딩 시 레이아웃이 더 이상 흔들리지 않음
* `개선` A-B 반복을 세 번 탭으로 A 지점 설정, B 지점 설정, 해제를 차례로 수행하도록 변경; 하단 컨트롤 영역의 간격과 아이콘 정렬도 함께 다듬음

# v1.2.2

###### 2026/08/27

* `수정` 새 버전 플러그인을 덮어쓰기 설치한 뒤 파일 관리자의 모든 오디오에서 잘못된 오디오 요청이라고 표시되던 문제 수정: 진입점이 이제 실행 중인 호스트가 캐시한 구버전 읽기 전용 요청과 호환
* `수정` 일부 기기의 비정상적인 MIME 표 때문에 `ogg`, `opus` 등 이미 지원되는 확장자가 잘못 거부되던 문제 수정: 확장자 허용 목록이 안정적인 표준 오디오 유형을 제공
* `개선` 요청이 거부되면 개인정보를 보호하는 사유 코드 로그를 남겨 (파일 이름, 경로, URI 미포함) 문제 보고와 원인 파악이 쉬워짐

# v1.2.1

###### 2026/08/27

* `기능` 파일 관리자에서 한 곡을 열면 같은 폴더의 다른 오디오를 자동으로 찾아 파일 이름의 자연 정렬 순서로 연속 재생: 최대 128곡, 선택한 곡부터 시작
* `수정` Android 7.0 기기에서 시스템이 자동으로 덧붙이는 창 플래그 때문에 정상적인 재생 요청이 잘못 거부되던 문제 수정
* `수정` 같은 폴더의 곡으로 자동 전환할 때 발생할 수 있던 충돌 문제 수정
* `개선` 같은 폴더 검색은 전 과정이 읽기 전용이며 범위가 제한됨: 하위 폴더로 재귀하지 않고 파일 위치를 추측하지 않으며 저장소 권한을 요청하지 않고, 오디오와 이름이 같은 동영상 파일은 대기열에 들어가지 않음
* `개선` 같은 폴더 읽기 세션은 백그라운드 재생 서비스가 보유하며 대기열 교체, 재생 완료, 서비스 종료 시 자동으로 닫힘
* `의존성` 내장 파일 관리자 동작 인터페이스를 하위 호환되는 v12로 업그레이드, 최소 호스트 버전은 build 5276 유지

# v1.2.0

###### 2026/08/27

* `기능` 다중 선택 재생 추가: 파일 관리자에서 최대 128개의 오디오를 선택해 한 번에 대기열에 넣고 선택한 순서를 그대로 유지
* `기능` 이전 곡 / 다음 곡 제어, 곡 이동과 삭제가 가능한 대기열 패널, 순차 / 셔플 / 한 곡 반복 모드 추가
* `기능` 취침 타이머 추가: 15 / 30 / 60분 프리셋 또는 사용자 지정 시간, 현재 곡을 마친 뒤 정지와 마지막 5초 페이드아웃 지원
* `기능` 선택한 구간을 반복해서 들을 수 있는 A-B 구간 반복 추가
* `개선` 시스템 미디어 제어에 이전 곡, 다음 곡, 10초 되감기 / 빨리 감기 명령을 추가하고 알림 메타데이터가 대기열 전환에 맞춰 실시간으로 갱신
* `개선` 파일 관리자 요청 검증을 순서 있는 다중 선택 대상까지 확장하면서 최소한의 읽기 전용 허가는 계속 유지
* `의존성` 파일 관리자 동작 인터페이스를 v2에서 v4로 업그레이드, 최소 호스트 버전을 build 5276으로 상향
* `의존성` AndroidX RecyclerView 1.4.0 추가

# v1.1.0

###### 2026/08/27

* `기능` 완전히 새로운 재생 화면: 앨범 아트, 제목 / 아티스트 / 앨범 태그, 기술 정보, 드래그 가능한 진행 막대
* `기능` 10초 되감기 / 빨리 감기, 한 곡 반복 전환, 0.5배부터 2배까지 배속 재생 추가
* `기능` 오디오에 내장된 태그와 커버를 자동으로 읽어 시스템 미디어 알림에 동기화
* `기능` 이어서 재생 추가: 같은 파일을 다시 열면 마지막 위치에서 이어지고 재생을 마치면 기록을 자동 삭제
* `개선` 재생이 끝나거나 디코딩에 실패한 뒤에도 화면에서 바로 다시 재생할 수 있어 파일 관리자로 돌아갈 필요 없음
* `개선` 재생 오류 안내에 구체적인 오류 코드를 포함해 문제 확인과 보고가 쉬워짐
* `의존성` AndroidX ConstraintLayout 2.2.1 추가
* `의존성` 더 이상 사용하지 않는 AndroidX Media3 UI 의존성 제거

# v1.0.1

###### 2026/08/08

* `수정` 플러그인 센터에서 플러그인을 활성화할 때 빈 서비스 바인딩이 나타나던 문제 수정
* `개선` 플러그인 이름, 설명, 사용 안내를 더 간결하고 자연스럽게 다듬음

# v1.0.0

###### 2026/08/02

* `기능` 첫 정식 버전: AutoJs6 파일 관리자에 오디오 재생 동작을 제공하고 18가지 일반 오디오 확장자를 지원하며 탭 한 번으로 재생
* `기능` Media3 ExoPlayer와 MediaSessionService 기반의 안정적인 재생: 백그라운드 재생, 오디오 포커스 관리, 출력 장치 분리 시 자동 일시중지, 시스템 미디어 제어 지원
* `기능` 다른 앱이 보내는 읽기 전용 오디오 열기 요청 수신 지원; 디코딩에 실패하면 다른 호환 앱으로 넘길 수 있으며 자기 반복은 방지
* `기능` 엄격한 보안 기준: 저장소와 인터넷 권한을 요청하지 않고 최소한의 읽기 전용 허가만 수락하고 전달
* `기능` 플러그인 정보, 사용 안내, README, 변경 기록을 10개 언어로 제공
* `의존성` AndroidX Media3 ExoPlayer, Session, UI 1.10.1 추가
